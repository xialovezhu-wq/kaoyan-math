# 批量导入

用于存放几百道历史错题的批次材料。这里只是临时导入区，不保存长期原始解析。

- `待处理/`：只放临时处理清单或编号范围，不长期保存原始解析。
- `已处理/`：已经拆分并生成错题卡的批次记录。
- `问题样本/`：编号不清、题干不全、答案缺失、无法判断科目的题。

每批拆成轻量错题卡后，应运行：

```bash
python3 错题知识网络/scripts/wrongnet.py rebuild
```

然后查看：

- `生成/错题索引.md`
- `生成/相似题清单.md`
